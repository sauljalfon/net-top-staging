# Development/staging environment - SQLite
# Use STAGING_DATABASE_URL for this app to avoid conflicts with other projects
# that export DATABASE_URL for PostgreSQL.
STAGING_DATABASE_URL=sqlite:///./net_top_staging.db
SHARED_PASSWORD=changeme
